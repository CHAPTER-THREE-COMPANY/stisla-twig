{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post.Title }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ post.Category }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post.Content }}</td>
            </tr>
            <tr>
                <th>Thumbnail</th>
                <td>{{ post.Thumbnail }}</td>
            </tr>
            <tr>
                <th>Tag</th>
                <td>{{ post.Tag }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ post.Status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post_index') }}">back to list</a>

    <a href="{{ path('post_edit', {'id': post.id}) }}">edit</a>

    {{ include('sample/post/_delete_form.html.twig') }}
{% endblock %}
