<div{{ attributes }}>
    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_row(form.savePath, {'attr': {'class': 'form-control', 'v-model': 'selectFile'}}) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">{{ button_label|default('保存') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>